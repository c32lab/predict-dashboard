# Predict API base URL (optional, defaults to '' for same-origin proxy)
# Set to full origin when the API runs on a different host, e.g.:
#   VITE_PREDICT_API_URL=http://new-server:18801
VITE_PREDICT_API_URL=
